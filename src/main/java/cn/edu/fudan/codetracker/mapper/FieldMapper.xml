<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.fudan.codetracker.mapper.FieldMapper">

    <insert id="insertFieldInfoList">
        insert into codeTracker.meta_field
        (
        uuid,
        simple_name,
        modifier,
        simple_type,
        init_value,
        class_uuid,
        class_name,
        file_path,
        start_commit,
        start_commit_date,
        end_commit,
        end_commit_date,
        repo_uuid,
        branch,
        version
        )
        VALUES
        <foreach collection="fieldNodes" item="item" index="index" separator=",">
            (
            #{item.uuid},
            #{item.simpleName},
            #{item.modifier},
            #{item.simpleType},
            #{item.initValue},
            #{item.parent.uuid},
            #{item.parent.className},
            #{item.filePath},
            #{commonInfo.startCommit},
            #{commonInfo.startCommitDate},
            #{commonInfo.endCommit},
            #{commonInfo.endCommitDate},
            #{commonInfo.repoUuid},
            #{commonInfo.branch},
            #{item.version}
            )
        </foreach>
    </insert>

    <insert id="insertRawFieldInfoList">
        insert into codeTracker.raw_field
        (
        uuid,
        simple_name,
        simple_type,
        modifier,
        init_value,
        class_uuid,
        field_uuid,
        version,
        change_relation,
        commitid,
        committer,
        commit_date,
        commit_message,
        parent_commit,
        full_name,
        repo_uuid,
        branch
        )
        VALUES
        <foreach collection="fieldNodes" item="item" index="index" separator=",">
            (
            #{item.uuid},
            #{item.simpleName},
            #{item.modifier},
            #{item.simpleType},
            #{item.initValue},
            #{item.parent.uuid},
            #{item.rootUuid},
            #{item.version},
            #{item.changeStatus},
            #{commonInfo.commit},
            #{commonInfo.committer},
            #{commonInfo.commitDate},
            #{commonInfo.commitMessage},
            #{commonInfo.parentCommit},
            #{item.fullName},
            #{commonInfo.repoUuid},
            #{commonInfo.branch}
            )
        </foreach>
    </insert>

    <update id="updateChangeInfo">
        update codeTracker.meta_field
        <trim prefix="set" suffixOverrides=",">
            <trim prefix="end_commit = case" suffix="end,">
                <foreach collection="fieldNodes" item="item">
                    when uuid=#{item.rootUuid} then #{commonInfo.endCommit}
                </foreach>
            </trim>
            <trim prefix="end_commit_date = case" suffix="end,">
                <foreach collection="fieldNodes" item="item">
                    when uuid=#{item.rootUuid} then #{commonInfo.endCommitDate}
                </foreach>
            </trim>
            <trim prefix="version = case" suffix="end,">
                <foreach collection="fieldNodes" item="item">
                    when uuid=#{item.rootUuid} then #{item.version}
                </foreach>
            </trim>
        </trim>
        where uuid in
        <foreach collection="fieldNodes" index="index" item="item"
                 separator="," open="(" close=")">
            #{item.rootUuid}
        </foreach>
    </update>



    <resultMap id="trackerInfo" type="cn.edu.fudan.codetracker.domain.projectinfo.TrackerInfo">
        <id property="version" column="version"/>
        <id property="rootUUID" column="uuid"/>
    </resultMap>

    <select id="getTrackerInfo" resultMap="trackerInfo" parameterType="String">
        select version,uuid from codeTracker.meta_field where file_path = #{filePath} and class_name = #{className} and simple_name = #{simpleName} and repo_uuid = #{repoUuid} and branch = #{branch} order by version desc limit 1
    </select>

</mapper>